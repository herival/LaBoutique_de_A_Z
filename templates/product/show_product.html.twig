{% extends 'base.html.twig' %}

{% block title %}{{product.name}}{% endblock %}

{% block content %}
    

    <div class="row">
        <div class="col-md-4">
            <img src="{{ asset ('assets/uploads/') }}{{product.illustration}}" alt="{{ asset ('assets/uploads/') }}{{product.illustration}}" class="img-fluid">
        </div>
        <div class="col-md-8 my-auto">
            <h3 class="h3">{{product.name}}</h3>
            <p>{{product.subtitle}}</p>
            <span class="h5 font-weight-bold">{{ (product.price / 100) | number_format(2, ',', '.')}} €</span>
            <hr>
            <p> {{product.description}}</p>

            <a href="{{ path ('add_to_cart' , {'id' :  product.id })}}" class="btn btn-primary">Ajouter au panier</a>

        </div>
    </div>



{% endblock %}
